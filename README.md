This Python project converts any user-entered text into the number key sequences used on old mobile phones (similar to the T9 system). Each letter is represented as a series of keypresses based on the classic mobile keypad layout.

>> Features
Maps each letter to its corresponding number key on a classic phone keypad.

Returns the result as a list of numbers.

Supports lowercase letters and spaces.

Simple and easy-to-understand structure.
